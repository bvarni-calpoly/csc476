- fix proj matrix for portals
- fix which portaldrawframe function to use (automate portal rendering placement)
- add animation through multiple obj files
    - using a vertex shader to lerp
- add lighting
- add a framebuffer kernel for objecet outlining
- add a rocket jumper
- add raycasts?
- add enemy
- add backface culling or frustrum culling or whatever the single opengl command is
- fix obj loader to also read mtl values
- add broadphase and narrowphase collsiion type (aabb to plane)


-- FOR TODAY / SOON --
    change functions to iterate by reference and not value
    
    glUniform3fv(scene->texProg->getUniform("lightPos"), 1, glm::value_ptr(callbacks->lightTrans)); FIX THIS LINE

    move camera player movements into player class
    make a function to load obj files
    put render shaders/programs in their own functions
    cleanup render
    test out a basic map with moving objects
    frustrum culling

-- FOR ANOTHER DAY --
lookat function/matrix
-make a function for drawing collision shapes (like drawSkybox)
-move init functions to another class
-add rotations to aabb
-add basic map loading (add mtl later)

make callback for toggle mouse movement so imgui can be clicked

-- FOR TOMORROW --
-add aabb collision to camera/map

-- LONG TERM --
Setup
    Review final project code for improvements to
        camera
        window dimensions
        just compare the two files

Shaders
    Write shaders from scratch again
    Blinn-Phong lighting
    Add shadows
    Frustum Culling

Optimizations
    Forward declarations versus Full include
    Pass addresses instead of values
    Having memory be sequential or near each other for objects instead of pointers at random memory addresses