TODAY
rocket jumper
teleport objects

MONDAY
add recoil animation
sensitivity of mouse
moving platforms
moving portals

TUESDAY
backface culling
fix collision tunneling
add turrets / enemies that respond to rockets
have enemies face direction of player
fan movement
chainsaw
fix proj matrix for portals

WEDNESDAY
make map
refine jumps
surf through portal
wall

-normal vec wall walking

- add texture mapping / loading
- fix which portaldrawframe function to use (automate portal rendering placement)
- add animation through multiple obj files
    - using a vertex shader to lerp / mix
- add raycasts?
- add enemy
- add backface culling or frustrum culling or whatever the single opengl command is
- fix obj loader to also read mtl values
    - clean up the normal loading for loadMapGeom()

attributes versus uniforms

add backface culling <<<

known issues
- fov calculations
- reload animations
- if there is no skybox all the rendered portals scene show
- currently framebuffers are having the window dimensions multiplied by 2 to fit 2k displays
- portal_entrance_n must come before portal_exit_n in obj file

collision
- currently convex hull is checking every vector four times...

for portal collision check if the plane is vertical for the threshold

change functions to iterate by reference and not value

frustrum culling
lookat function/matrix

Optimizations
    Forward declarations versus Full include
    Pass addresses instead of values
    Having memory be sequential or near each other for objects instead of pointers at random memory addresses